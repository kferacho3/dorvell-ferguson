.next/
node_modules/
dist/
out/
*.tsbuildinfo
public/dorvell/originals/
# Raw, unoptimized source media — never shipped.
assets/raw/
# Optimized creative video is served from S3 (see resolveCreativeAsset default);
# `npm run optimize:videos` writes here locally, then `npm run upload:assets` ships
# it to the bucket. Kept out of git so the repo/deploy stays light.
public/dorvell/videos/
.env*.local
.DS_Store
tmp/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
